using Profissional.Infra;
using Profissional.Repositorio;
using Profissional.Servico;
using System;
using System.Linq;

namespace teste
{
    class Program
    {
        //static void Main(string[] args)
        ////{
        ////    var servico = new ServicoTipoServico().Get();
        ////    foreach (var item in servico)
        ////    {
        ////        Console.WriteLine(item.ID +"  |  "+ item.Descricao +"   |   "+ item.DataCriacao);
        ////        Console.WriteLine("------------------------------------------------------------------------ ");
        ////    }

        ////    var data = new Contexto().ServicoTipo.ToList();
        ////    var teste = new ServicoTipoRep().GetAll();

        ////    foreach (var item in teste)
        ////        Console.WriteLine(item.Descricao);


        //    Console.WriteLine("Hello World!");
        //}
    }
}
